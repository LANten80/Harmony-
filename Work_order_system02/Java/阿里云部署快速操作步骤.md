# 阿里云部署快速操作步骤

## 🚀 快速部署流程（按顺序执行）

### 第零步：检测系统类型（重要！）

**在开始之前，先确定你的服务器系统类型：**

```bash
# 检测系统类型
cat /etc/os-release
# 或者
cat /etc/redhat-release  # CentOS/RHEL系统会显示版本信息
```

**判断方法：**
- 如果显示 `CentOS`、`Red Hat`、`Rocky` 等 → 使用 **CentOS/RHEL** 的命令（使用 `yum`）
- 如果显示 `Ubuntu`、`Debian` 等 → 使用 **Ubuntu/Debian** 的命令（使用 `apt`）

**或者直接测试：**
```bash
# 测试是否有yum命令（CentOS/RHEL）
which yum
# 如果有输出，说明是CentOS/RHEL系统

# 测试是否有apt命令（Ubuntu/Debian）
which apt
# 如果有输出，说明是Ubuntu/Debian系统
```

**⚠️ 根据你的截图，你的服务器是 CentOS/RHEL 系统，请使用 `yum` 命令！**

---

### 第一步：连接服务器
```bash
ssh root@你的服务器IP
# 输入密码
```

### 第二步：安装环境（一次性操作）

#### 2.1 更新系统

**根据你的系统类型选择对应的命令：**

```bash
# ⭐ CentOS/RHEL 系统（你的服务器使用这个）
yum update -y && yum install -y wget curl vim git

# Ubuntu/Debian 系统
apt update && apt upgrade -y && apt install -y wget curl vim git
```

#### 2.2 安装JDK 17

**根据你的系统类型选择：**

```bash
# ⭐ CentOS/RHEL 系统（你的服务器使用这个）
yum install -y java-17-openjdk java-17-openjdk-devel

# Ubuntu/Debian 系统
apt install -y openjdk-17-jdk

# 验证（所有系统通用）
java -version
```

#### 2.3 安装Maven

**方法1：使用yum安装（推荐，简单快速）**

```bash
# ⭐ CentOS/RHEL 系统（你的服务器使用这个）
yum install -y maven

# 验证
mvn -version
```

**方法2：手动下载安装（如果需要特定版本）**

如果yum安装的版本不符合要求，可以手动下载：

```bash
cd /usr/local

# 尝试下载Maven 3.9.5（稳定版本）
wget https://archive.apache.org/dist/maven/maven-3/3.9.5/binaries/apache-maven-3.9.5-bin.tar.gz

# 如果上面链接失败，尝试使用镜像站点
# wget https://mirrors.tuna.tsinghua.edu.cn/apache/maven/maven-3/3.9.5/binaries/apache-maven-3.9.5-bin.tar.gz

# 解压
tar -xzf apache-maven-3.9.5-bin.tar.gz
mv apache-maven-3.9.5 maven

# 配置环境变量
echo 'export MAVEN_HOME=/usr/local/maven' >> /etc/profile
echo 'export PATH=$MAVEN_HOME/bin:$PATH' >> /etc/profile
source /etc/profile

# 验证
mvn -version
```

**如果下载仍然失败，可以访问Maven官网获取最新下载链接：**
- 官网：https://maven.apache.org/download.cgi
- 或者使用国内镜像：https://mirrors.tuna.tsinghua.edu.cn/apache/maven/

#### 2.4 安装MySQL

**根据你的系统类型选择：**

##### CentOS/RHEL 系统安装MySQL

**方法1：修复GPG密钥问题后安装（推荐）**

如果遇到GPG密钥错误，先修复：

```bash
# 1. 下载MySQL仓库RPM包
wget https://dev.mysql.com/get/mysql80-community-release-el7-3.noarch.rpm

# 2. 安装仓库（如果已安装，先删除再重新安装）
rpm -e mysql80-community-release 2>/dev/null || true
rpm -ivh mysql80-community-release-el7-3.noarch.rpm

# 3. 重新导入GPG密钥
rpm --import https://repo.mysql.com/RPM-GPG-KEY-mysql-2022

# 4. 如果上面密钥导入失败，尝试导入所有MySQL密钥
rpm --import https://repo.mysql.com/RPM-GPG-KEY-mysql

# 5. 清理yum缓存
yum clean all

# 6. 安装MySQL服务器
yum install -y mysql-server

# 7. 启动MySQL服务
systemctl start mysqld
systemctl enable mysqld

# 8. 查看临时密码（重要！）
grep 'temporary password' /var/log/mysqld.log
```

**方法2：跳过GPG检查安装（临时方案，不推荐用于生产环境）**

如果方法1仍然失败，可以临时跳过GPG检查：

```bash
# 安装时跳过GPG检查
yum install -y mysql-server --nogpgcheck

# 启动MySQL服务
systemctl start mysqld
systemctl enable mysqld

# 查看临时密码
grep 'temporary password' /var/log/mysqld.log
```

**方法3：使用MariaDB（MySQL的替代品，完全兼容，更简单）**

如果MySQL安装一直有问题，可以使用MariaDB，它与MySQL完全兼容：

```bash
# 安装MariaDB
yum install -y mariadb-server mariadb

# 启动MariaDB服务
systemctl start mariadb
systemctl enable mariadb

# 运行安全配置脚本（设置root密码）
mysql_secure_installation
```

**注意：** 使用MariaDB时，后续所有MySQL命令都改为 `mysql`（不是 `mysqld`），其他使用方式完全相同。

##### Ubuntu/Debian 系统安装MySQL

```bash
apt install -y mysql-server
systemctl start mysql
systemctl enable mysql
mysql_secure_installation
```

### 第三步：配置数据库

**重要：MySQL 8.0要求使用临时密码登录后，必须先修改密码才能执行其他操作！**

```bash
# 1. 使用临时密码登录MySQL
mysql -u root -p
# 输入临时密码：u*xDApfsH9N-（或你获取到的临时密码）
```

**登录成功后，立即修改root密码（必须！）：**

```sql
-- 修改root密码（请将'你的新密码'改为一个强密码，至少12位）
ALTER USER 'root'@'localhost' IDENTIFIED BY '你的新密码';

-- 刷新权限
FLUSH PRIVILEGES;
```

**然后创建数据库和用户：**

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS work_order_system DEFAULT CHARACTER SET utf8mb4 DEFAULT COLLATE utf8mb4_unicode_ci;

-- 创建专用用户（请将'你的数据库密码'改为一个强密码）
CREATE USER 'workorder'@'localhost' IDENTIFIED BY '你的数据库密码';

-- 授权
GRANT ALL PRIVILEGES ON work_order_system.* TO 'workorder'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 退出MySQL
exit;
```

**验证新用户（可选）：**

```bash
# 使用新创建的用户登录测试
mysql -u workorder -p work_order_system
# 输入你设置的密码

# 如果登录成功，执行
SHOW TABLES;
exit;
```

**导入数据库脚本（需要先上传init.sql到服务器）：**

#### 上传init.sql文件的方法

**方法1：使用WinSCP（Windows推荐）**

1. 打开WinSCP，连接到服务器
2. 左侧：找到本地文件 `Java/database/init.sql`
3. 右侧：进入服务器目录 `/opt/work-order-system`
4. 将 `init.sql` 拖拽到右侧，或右键选择"上传"

**方法2：使用scp命令（命令行）**

在本地项目根目录执行：

```bash
# Windows PowerShell 或 CMD
scp Java/database/init.sql root@你的服务器IP:/opt/work-order-system/

# 或者使用完整路径
scp D:\GitHub\Harmony-\Work_order_system02\Java\database\init.sql root@你的服务器IP:/opt/work-order-system/
```

**方法3：在服务器上直接创建（如果文件内容不多）**

如果文件较小，可以在服务器上直接创建：

```bash
# 在服务器上执行
mkdir -p /opt/work-order-system
vim /opt/work-order-system/init.sql
# 然后粘贴init.sql的内容，保存退出（:wq）
```

#### 导入数据库脚本

上传完成后，在服务器上执行：

```bash
# 将 /opt/work-order-system/init.sql 改为实际的文件路径
mysql -u workorder -p work_order_system < /opt/work-order-system/init.sql
# 输入workorder用户的密码
```

### 第四步：上传项目

#### 方法1：使用WinSCP（Windows）
- 连接服务器
- 上传 `Java` 文件夹到 `/opt/work-order-system`

#### 方法2：使用scp命令
```bash
# 在本地执行
scp -r Java root@你的服务器IP:/opt/work-order-system
```

### 第五步：编译和配置项目

```bash
# 进入项目目录
cd /opt/work-order-system/Java

# 修改配置文件
vim src/main/resources/application.yml
```

**必须修改的配置：**
```yaml
spring:
  datasource:
    username: workorder  # 改为数据库用户名
    password: 你的数据库密码  # 改为数据库密码

jwt:
  secret: 你的安全密钥  # 改为至少32位的随机字符串
```

**保存退出：** 按 `Esc`，输入 `:wq`，按回车

```bash
# 编译打包
mvn clean package -DskipTests

# 创建运行目录
mkdir -p /opt/work-order-system/app
mkdir -p /opt/work-order-system/logs

# 复制JAR文件
cp target/work-order-system-1.0.0.jar /opt/work-order-system/app/
```

### 第六步：配置防火墙

**根据你的系统类型选择：**

```bash
# ⭐ CentOS/RHEL 系统（你的服务器使用这个）
firewall-cmd --permanent --add-port=8080/tcp
firewall-cmd --reload
firewall-cmd --list-ports  # 验证端口已开放

# Ubuntu/Debian 系统
ufw allow 8080/tcp
ufw enable
ufw status  # 验证端口已开放
```

### 第七步：配置阿里云安全组

1. 登录阿里云控制台：https://ecs.console.aliyun.com
2. 进入ECS实例 → 安全组
3. 添加规则：
   - 端口：8080/8080
   - 授权对象：0.0.0.0/0
   - 保存

### 第八步：创建系统服务

```bash
# 创建服务文件
vim /etc/systemd/system/work-order-system.service
```

**文件内容：**
```ini
[Unit]
Description=Work Order System Backend Service
After=network.target mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/work-order-system/app
ExecStart=/usr/bin/java -jar -Xms512m -Xmx1024m /opt/work-order-system/app/work-order-system-1.0.0.jar
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**保存退出后执行：**
```bash
# 启用服务
systemctl daemon-reload
systemctl enable work-order-system
systemctl start work-order-system

# 查看状态
systemctl status work-order-system

# 查看日志
journalctl -u work-order-system -f
```

### 第九步：验证部署

```bash
# 测试服务
curl http://localhost:8080/api/health

# 或在浏览器访问
http://你的服务器IP:8080/api
```

---

## 📋 常用命令速查

### 服务管理
```bash
systemctl start work-order-system      # 启动
systemctl stop work-order-system       # 停止
systemctl restart work-order-system    # 重启
systemctl status work-order-system     # 查看状态
systemctl enable work-order-system     # 开机自启
systemctl disable work-order-system    # 取消开机自启
```

### 日志查看
```bash
journalctl -u work-order-system -f           # 实时日志
journalctl -u work-order-system -n 100       # 最近100行
tail -f /opt/work-order-system/logs/*.log    # 应用日志文件
```

### 进程检查
```bash
ps aux | grep java                    # 查看Java进程
netstat -tlnp | grep 8080            # 查看端口占用
systemctl status mysql                # 查看MySQL状态
```

### 数据库操作
```bash
mysql -u workorder -p work_order_system      # 登录数据库
systemctl start mysql                         # 启动MySQL
systemctl stop mysql                          # 停止MySQL
```

---

## ⚠️ 重要提醒

1. **修改默认密码**：首次登录后立即修改root密码和数据库密码
2. **JWT密钥**：生产环境必须修改为复杂的随机字符串
3. **数据库密码**：不要使用弱密码
4. **安全组**：生产环境建议限制IP访问，不要使用0.0.0.0/0
5. **定期备份**：配置数据库自动备份

---

## 🔧 故障排查

### 服务无法启动
```bash
journalctl -u work-order-system -n 50    # 查看错误日志
java -jar /opt/work-order-system/app/work-order-system-1.0.0.jar  # 手动运行查看错误
```

### 无法访问
```bash
systemctl status work-order-system       # 检查服务状态
netstat -tlnp | grep 8080               # 检查端口
firewall-cmd --list-ports               # 检查防火墙
```

### 数据库连接失败
```bash
systemctl status mysql                   # 检查MySQL状态
mysql -u workorder -p work_order_system  # 测试连接
```

---

**详细文档请参考：** `阿里云Linux服务器部署完整教程.md`

